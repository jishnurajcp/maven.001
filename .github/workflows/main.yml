name: demo1

on:
  workflow_dispatch:

jobs:
  build-using-maven:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build using Maven
        run: mvn -B package

      - name: List the artifact
        run: ls -lh target/
